name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: testVar=test
    - name: Run a multi-line script
      run: |
        echo $testVar
